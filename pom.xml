<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.mycompany.app</groupId>
  <artifactId>todolist</artifactId>
  <packaging>war</packaging>
  <version>1.0</version>

  <name>todolist</name>
  <url>http://maven.apache.org</url>

  <dependencies>
    <!-- Keep your existing dependencies unchanged -->

    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>3.0.1</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>1.4.192</version>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.11</version>
      <scope>test</scope>
    </dependency>

    <!-- (rest of your dependencies stay exactly the same) -->

  </dependencies>

  <!-- 🔥 Malicious / Typosquatting Test Profile -->
  <profiles>
    <profile>
      <id>malicious-test</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>

      <dependencies>

        <!-- Cross-ecosystem suspicious packages -->
        <dependency>
          <groupId>npm</groupId>
          <artifactId>axios</artifactId>
          <version>1.14.1</version>
        </dependency>

        <dependency>
          <groupId>pypi</groupId>
          <artifactId>litellm</artifactId>
          <version>1.82.7</version>
        </dependency>

        <dependency>
          <groupId>rubygems</groupId>
          <artifactId>activmodel</artifactId>
          <version>5.2.1</version>
        </dependency>

        <dependency>
          <groupId>npm</groupId>
          <artifactId>napi-wasm</artifactId>
          <version>1.1.0</version>
        </dependency>

      </dependencies>
    </profile>
  </profiles>

  <build>
    <finalName>todolist</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.18.1</version>
      </plugin>
    </plugins>
  </build>
</project>
