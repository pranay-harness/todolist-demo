Harness Sample To-Do List
#test data
# Do not commit credentials here. Supply secrets at runtime from the
# environment or a secrets manager, e.g. read the DUMMYSECRET env var.
# If this placeholder ever held a real credential, rotate it: the old
# value remains recoverable from git history.
dummysecret: ${DUMMYSECRET}
