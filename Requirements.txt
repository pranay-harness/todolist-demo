Harness Sample To-Do List
#test data
# The value below is supplied at runtime from the environment / secrets manager.
# The literal that used to be committed here must be rotated (revoked and reissued),
# because it is still present in git history.
dummysecret: ${DUMMY_SECRET}
