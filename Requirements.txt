Harness Sample To-Do List
#test data
# Credential removed from source control (was committed here, unused by any code or CI).
# If a value is ever needed, supply it at runtime from an environment variable or a
# secrets manager - do not commit a literal.
# ROTATE: the previously committed credential still exists in git history and must be
# revoked and reissued.
